import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#494a54',
        flex: 1
    },
    
    buttons: {
        flexDirection: 'row',
        flexWrap: 'wrap'
    }
});