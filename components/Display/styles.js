import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 40
    },

    title: {
        fontSize: 20,
        color: 'white',
        textAlign: 'center'
    },

    value: {
        marginTop: 20,
        padding: 10,
        fontSize: 50,
        fontWeight: 'bold',
        color: 'white',
        textAlign: 'right'
    }
});